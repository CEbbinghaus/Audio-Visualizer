export default class BaseAnalyzer {
	/**
	 * Initializes the Analyzer
	 * @memberOf WebAnalyzer
	 */
	async Initialize() {}

	/**
	 * Processes and returns Spectrum Array
	 * @returns {Array<number>}
	 * @memberOf WebAnalyzer
	 */
	Tick() {}
}
